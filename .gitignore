# build outputs (regenerated by tools/*)
dist/

# local QA / scratch artifacts
screenshots/
folder/
*.backup*
src-deck/content/*.backup*
tmp-*

# os / editor
.DS_Store
**/.DS_Store
node_modules/

# portable Node runtime downloaded by install-*.sh (never commit)
.node/

# server.js safeWrite backups (runtime artifacts)
*.bak
